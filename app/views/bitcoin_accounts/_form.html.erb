<%= form_for(@bitcoin_account) do |f| %>
  <% if @bitcoin_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bitcoin_account.errors.count, "error") %> prohibited this bitcoin_account from being saved:</h2>

      <ul>
      <% @bitcoin_account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
